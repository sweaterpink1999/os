# =========================================
# Xray Nginx Config - Full 443 WS + gRPC (Stable)
# IPv4-Only | Optimized | Anti-Port-Bentrok
# =========================================

server {
    # 🌐 HTTP WS Ports (tanpa SSL)
    listen 80 reuseport;         # Port utama (Cloudflare/CDN)
    listen 8080 reuseport;       # Alternatif HTTP
    listen 8880 reuseport;       # Alternatif HTTP
    listen 2082 reuseport;       # Tambahan aman (ganti 2052)
    listen 2086 reuseport;       # Alternatif umum (cPanel)
    listen 2095 reuseport;       # Alternatif HTTP (mail/ws)

    # 🔒 HTTPS WS + gRPC (SSL)
    listen 443 ssl http2 reuseport;     # Port utama HTTPS
    listen 8443 ssl http2 reuseport;    # Alternatif SSL
    listen 2053 ssl http2 reuseport;    # Alternatif SSL Cloudflare
    listen 2087 ssl http2 reuseport;    # Alternatif SSL
    listen 2096 ssl http2 reuseport;    # Alternatif SSL

    server_name yourdomain.com;

    # =========================
    # SSL Config
    # =========================
    ssl_certificate /etc/xray/xray.crt;
    ssl_certificate_key /etc/xray/xray.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1h;

    # =========================
    # Header & Security
    # =========================
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # =========================
    # Performance
    # =========================
    client_max_body_size 20M;
    keepalive_timeout 30;
    tcp_nopush on;
    tcp_nodelay on;
    sendfile on;

    proxy_buffering off;
    proxy_request_buffering off;
    proxy_read_timeout 3600s;
    proxy_send_timeout 3600s;
    proxy_connect_timeout 60s;
    resolver 1.1.1.1 8.8.8.8 valid=300s ipv6=off;
    resolver_timeout 10s;

    # =========================
    # 🟦 WebSocket
    # =========================
    location /vless {
        proxy_pass http://127.0.0.1:10001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }

    location /vmess {
        proxy_pass http://127.0.0.1:10002;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }

    location /trojan-ws {
        proxy_pass http://127.0.0.1:10003;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }

    location /ss-ws {
        proxy_pass http://127.0.0.1:10004;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }

    # =========================
    # 🟩 gRPC
    # =========================
    location /vless-grpc  { grpc_pass grpc://127.0.0.1:10005; grpc_set_header Host $host; }
    location /vmess-grpc  { grpc_pass grpc://127.0.0.1:10006; grpc_set_header Host $host; }
    location /trojan-grpc { grpc_pass grpc://127.0.0.1:10007; grpc_set_header Host $host; }
    location /ss-grpc     { grpc_pass grpc://127.0.0.1:10008; grpc_set_header Host $host; }

    # =========================
    # 🟨 Default Tunnel (SSH/OVPN)
    # =========================
    location / {
        proxy_pass http://127.0.0.1:10015;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_buffering off;
    }
}

# =========================
# 🧱 HTTPS Page (port 81)
# =========================
server {
    listen 81 ssl http2 reuseport;
    ssl_certificate /etc/xray/xray.crt;
    ssl_certificate_key /etc/xray/xray.key;
    root /var/www/html;
    index index.html;
}
